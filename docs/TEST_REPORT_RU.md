## Suite 0.8.0 — 10 сентября 2026

Локально собраны Districts 0.1.0, Builds 0.7.8 и Population 0.1.1 на Java 25 / Gradle 9.0.0. DistrictsSmoke, PopulationSmoke и все smoke-задачи Builds завершились успешно. В комплект включены три новых JAR и 13 неизменённых модулей из проверенного релиза 0.7.9.

DistrictsSmoke проверяет семь типов и 91 профиль, выделение прямоугольника, отрицательные координаты, другой размер сетки Towny, чужую землю, пересечения, связность сторонами, лимиты до перебора большой области, завершённость, полную площадку, промышленную связку в одном районе, нейтральный результат при неверном районе или потере земли и ограничение коэффициента. Для производства проверены вероятностное округление и заполненный выходной слот.

Сохранение проверено на временной базе: перезапуск, удаление, отказ при изменении размера сетки, повреждённый YAML без перезаписи и ошибка записи без изменения памяти. BuildingFootprintsSmoke проверил все 91 проекта в четырёх поворотах (364 площадки), выступы, двор и запрет считать недостроенное здание завершённым по одному сохранённому уровню.

PopulationSmoke дополнительно проверил бонус жилья (120 × 1.15 + 20 = 158), снабжения пекарни, отсутствие бонуса у недостроенного квартала и нейтральный коэффициент при NaN. Прежние проверки роста, дефицита, занятости, сохранения, моделей, ресурсов, крыш, иконок, материалов и мобилизации продолжают проходить. Каталог остаётся 80 зданий / 11 чудес / 411 уровней; архитектура и ресурсные требования не меняются.

Пакет проверяется scripts/verify_release.py: 16 JAR, версии, целостность, порядок загрузки без циклов, шесть словарей, интеграционные классы Districts в Builds/Population, ресурсы нового модуля и русский каталог всех проектов. CI выполняет полную сборку всех 16 модулей и smoke-задачи Districts перед публикацией.

Границы проверки: игровой сервер не запускался. Меню, частицы границ, фактическая обработка печей/культур и взаимодействие с установленными на сервере плагинами не проверялись в Minecraft. Проверены компиляция API и обработчики по коду: права, свой город, отменённые события, вместимость результата и отложенные действия меню.

## Suite 0.7.9 — 9 сентября 2026

Локально собраны Population 0.1.0 и Builds 0.7.7 на Java 25 / Gradle 9.0.0. PopulationSmoke и все smoke-задачи Builds успешно выполнены. Пакет из 15 JAR (два новых, 13 неизменённых из проверенного релиза 0.7.8) прошёл `scripts/verify_release.py`: имена, версии, целостность, порядок загрузки, шесть словарей и полный каталог профилей населения.

PopulationSmoke проверяет 91 профиль, стартовое снабжение, отсутствие мощности у недостроенных зданий, ввод жилья на V этапе, питание от пекарни и аграрного комплекса, воду от башни и резервуара, чудеса I уровня, ограничение лишних уровней, занятость и довольство. Проверены умеренный и критический дефицит еды/воды, граница 75%, постепенная убыль при потере жилья, дробный прирост, вместимость, сокращение до нуля и повторное заселение, смена знака прироста, нулевые коэффициенты, пользовательские проекты, наследование новых настроек с сохранением нулевых значений администратора и отклонение NaN.

Сохранение проверено на отдельной временной базе: UUID, количество, дробный остаток и прошлое изменение сохраняются; перезапуск переносит время следующего расчёта без списания жителей; удалённый город очищается; повреждённый YAML вызывает отказ загрузки без перезаписи файла.

Builds: 80 зданий и 11 чудес, 411 уровней. Жилой квартал проходит проверки пяти последовательных этапов, входа, категории, уникальной геометрии и ресурсов. Прежние 26 исправленных импортированных моделей сохраняют 1937 добавленных опор крыш; изменения старых чертежей в этом выпуске отсутствуют.

Границы проверки: игровой сервер не запускался. Графический интерфейс, фактическая загрузка Towny/Builds/PlaceholderAPI и внешний вид жилого квартала не проверялись в Minecraft. Сборка проверяет совместимость с API; обработчики меню проверены по коду на права, гражданство, перенос предметов и отложенное открытие.

## Suite 0.7.8 — 9 сентября 2026

Локально успешно собраны Builds 0.7.6, Events 0.2.0 и Expeditions 0.1.9 на Java 25 / Gradle 9.0.0 с Paper API 26.2.build.121-stable. Пройдены все smoke-задачи этих модулей. Пакет из 14 JAR проверен scripts/verify_release.py: имена, версии, целостность, зависимости и совпадение русских словарей с исходниками.

Каталог: 79 зданий и 11 чудес, 406 уровней. Проверены стадии, ресурсы и сохранение прежних архитектур. Для 26 импортированных моделей добавлено 1937 опорных/стеновых блоков; исходные 25 497 координат сохранены. Итоговый объём 37 импортированных моделей — 27 434 блока. Дополнительно закрыты стеклянные фронтоны и изменён порядок строительства фронтонов обычных домов. Контрольные срезы мельницы, собора, теплицы и Хрустального дворца сопоставлены с прежней геометрией; обнаруженный дефект отдельных фронтонов включён в исправление.

Регрессии: различимые иконки и миграция старого конфига; права экспедиций и их отзыв; сохранение, расходование и граница срока возврата; 18 лет включительно, неизвестный/неверный возраст, права офицера, принадлежность к городу, наличие штаба, повторная мобилизация и лимит мест. Модель состояния рейда проверена на 10 усиливающихся волнах и 100 смертях: повторные смерти, поставки до завершения, перезапуск на пятой волне, запрет 11-й волны и замена старого префикса.

Границы: тесты не запускали мир пользователя, не рендерили модели ModelEngine/ItemsAdder в Minecraft и не проверяли конкретный плагин паспорта. Эти интеграции используют публичные API и собственные ID/placeholder администратора; см. [инструкцию обновления](UPDATE_0.7.8_RU.md).

# Отчёт проверки NeverLand Towny Suite 0.7.2

- Все 14 модулей собираются GitHub Actions на Java 25 с целевым bytecode Java 17.
- NeverLandTownyBuilds проходит 15 исполняемых smoke-тестов моделей, геометрии, локализации, ресурсов, совместимости, миграции требований и муниципального расширения.
- Тесты ресурсов отдельно проверяют одинаковый результат подсчёта, списания и фонда для ванильного материала со служебными компонентами, а также изоляцию ItemsAdder/NBT-предметов.
- Подтверждены 78 обычных проектов, 11 Чудес Света, 7 категорий и 89 физических проектов.
- Все 11 Чудес проверяются на уникальную геометрию, фундамент, лестницы, проходимые двери, ручные несущие блоки и автоматическую отделку.
- Для всех 11 Чудес проверяются точные наборы из трёх существующих городских зданий и требуемые уровни — всего 33 инфраструктурных условия.
- Повторно проверяются 37 импортированных моделей: 185 стадий и 25 497 блоков.
- Archaeology содержит 28 артефактов, 10 коллекций и требования для всех 11 Чудес.
- Конфигурационный тест разбирает 59 YAML-файлов и сверяет версии 14 модулей.
- Проверяются уникальность Bukkit-команд, целостность JAR, `plugin.yml`, главный класс и отсутствие циклов загрузки.
- В JAR не упаковываются классы Paper, Towny, Adventure или PlaceholderAPI.

Публикация релиза выполняется только после успешного прогона Verify для PR, повторной проверки основной ветки и отдельной сборки релизной ветки. Отдельно проверяются миграция отсутствующих `requires` и единое распознавание `DEEPSLATE_TILES` в фонде строительства.
