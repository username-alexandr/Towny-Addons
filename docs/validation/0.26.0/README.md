# Проверка 0.26.0

2026-09-13: отдельный сервер Purpur 26.2-2632, Towny 0.103.2.0, Java 25.0.4.1, Vault 1.7.3, EssentialsX 2.22.0, PlaceholderAPI 2.12.3, Passport 0.4.0 и все 30 аддонов. Использованы локально собранные изменения и неизменённые JAR релиза 0.25.0; SHA-256 в `jars.json`.

- Первый запуск: 46 успешных проверок.
- Второй последовательный запуск: 13 успешных проверок.
- После каждой остановки на диске подтверждены все четыре назначения.
- Проверены реальные permissions Bukkit, Towny, интеграции шести аддонов, гражданство, смена мэра, выход/повторный вход, отказ диска и отключение плагина.
- Выборные советники сохраняются отдельно от назначенных министров.

Полный список: `checks.txt`. Исходники и повторяемый запуск — `scripts/council-runtime-probe`. Для проб нужен маркер `ALLOW_DISPOSABLE_RELIABILITY_PROBE`, JVM-флаг и адрес 127.0.0.1. Сначала `run.py --phase first`, затем отдельным вызовом `run.py --phase restart`; передайте `--server-dir` и `--java-home`. Существующие доказательства и повторное создание fixture не перезаписываются.

Player/InventoryView — тестовые прокси с настоящим PermissibleBase. Проверено поведение меню и блокировка перемещения предметов; визуальная читаемость конкретного клиентского ресурспака и нагрузочное тестирование сюда не входят. Рабочий сервер пользователя не изменялся.

Совет компилируется Gradle, его два executable smoke-теста и API-контракт проверены отдельно. Интеграционные JAR для стенда собраны javac 25 с `--release 17` и библиотеками этого сервера. Полная воспроизводимая Gradle/Maven-сборка всех модулей выполняется CI и workflow публикации.
