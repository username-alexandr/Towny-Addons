package ru.neverland.townyupkeep.api;
/** Test-only compatibility surface for the optional plugin loaded by reflection. */
public interface TownyUpkeepApi { boolean active(java.util.UUID town,String project); }
