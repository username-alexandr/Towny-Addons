name: NeverLandHorseUpgrades Test Build

on:
  push:
    branches:
      - chatgpt/neverland-horse-upgrades-test-build

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
      - name: Set up Gradle 9.1.0
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.1.0'
      - name: Build plugin
        working-directory: ci/NeverLandHorseUpgrades/project
        run: gradle --no-daemon clean build
      - name: Validate jar
        shell: bash
        run: |
          JAR=$(find ci/NeverLandHorseUpgrades/project/build/libs -maxdepth 1 -name '*.jar' ! -name '*sources*' | head -n1)
          test -n "$JAR"
          jar tf "$JAR" | grep -q '^plugin.yml$'
          jar tf "$JAR" | grep -q 'ru/neverland/horseupgrades/NeverLandHorseUpgradesPlugin.class'
          mkdir -p /tmp/horse-artifact
          cp "$JAR" /tmp/horse-artifact/NeverLandHorseUpgrades-0.2.1-test.jar
          sha256sum /tmp/horse-artifact/NeverLandHorseUpgrades-0.2.1-test.jar > /tmp/horse-artifact/NeverLandHorseUpgrades-0.2.1-test.jar.sha256
      - name: Upload test build
        uses: actions/upload-artifact@v4
        with:
          name: NeverLandHorseUpgrades-0.2.1-test
          path: /tmp/horse-artifact/*
          retention-days: 7
